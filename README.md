# behavioral-engagement-ML-backend
A Flask backend that predicts user engagement and ranks content recommendations from session behavio. built as a classification + recommendation system, not just a trained model.
